server {
  listen 80;

  root {{ document_root }}/{{ site_root }};
  index index.html index.htm;

  server_name {{ domain_name }};

  location / {
   default_type "text/html";
   try_files $uri.html $uri $uri/ =404;
  }
}
